# 代码规范文档

> **创建时间**: 2025年12月02日 23:50  
> **最后更新**: 2025年12月02日 23:50  
> **维护人**: Cursor AI

---

## 📋 总体原则

1. **文件行数限制**: 单个代码文件不超过 **300 行**
2. **组件复用**: 善于抽取公共组件，避免重复代码
3. **函数抽离**: 公共函数统一放入 `utils/` 目录
4. **类型定义**: 所有接口和类型统一放入 `types/` 目录
5. **代码注释**: 关键逻辑必须添加注释

---

## 📁 目录结构规范

### 小程序端 (`frontend/app/src/`)

```
src/
├── assets/           # 静态资源
│   └── icons/        # 图标文件（仅 PNG/JPG）
├── components/       # 公共组件（每个组件一个文件夹）
│   └── Button/
│       ├── index.tsx
│       ├── index.scss
│       └── index.config.ts
├── context/          # React Context
├── hooks/            # 自定义 Hooks
├── pages/            # 页面组件
│   └── index/
│       ├── index.tsx
│       ├── index.scss
│       └── index.config.ts
├── services/         # API 服务封装
├── store/            # 状态管理
├── styles/           # 全局样式
├── types/            # TypeScript 类型定义
├── utils/            # 工具函数
├── mock/             # Mock 数据
└── config/           # 配置文件
```

---

## 📝 命名规范

### 文件命名

| 类型 | 规范 | 示例 |
|------|------|------|
| 组件文件夹 | PascalCase | `Button/`, `EventCard/` |
| 组件文件 | index.tsx | `Button/index.tsx` |
| 样式文件 | index.scss | `Button/index.scss` |
| 工具函数 | camelCase | `formatDate.ts`, `storage.ts` |
| 类型文件 | camelCase | `activity.ts`, `user.ts` |
| 常量文件 | UPPER_CASE | `CONSTANTS.ts` |

### 变量命名

| 类型 | 规范 | 示例 |
|------|------|------|
| 组件名 | PascalCase | `EventCard`, `CustomTabBar` |
| 函数名 | camelCase | `handleClick`, `fetchData` |
| 变量名 | camelCase | `isLoading`, `userData` |
| 常量名 | UPPER_CASE | `API_BASE_URL`, `MAX_COUNT` |
| CSS 类名 | kebab-case | `event-card`, `user-header` |

---

## 🧩 组件规范

### 组件结构模板

```tsx
// 1. 导入语句（按顺序：React → Taro → 第三方 → 本地）
import { useState, useEffect } from 'react'
import { View, Text, Image } from '@tarojs/components'
import Taro from '@tarojs/taro'

// 2. 类型导入
import type { Activity } from '@/types/activity'

// 3. 样式导入
import './index.scss'

// 4. 图标导入
import IconCalendar from '@/assets/icons/calendar.png'

// 5. Props 类型定义
interface EventCardProps {
  activity: Activity
  onPress?: () => void
}

// 6. 组件定义
const EventCard: React.FC<EventCardProps> = ({ activity, onPress }) => {
  // 6.1 状态定义
  const [isLoading, setIsLoading] = useState(false)
  
  // 6.2 副作用
  useEffect(() => {
    // ...
  }, [])
  
  // 6.3 事件处理函数
  const handlePress = () => {
    onPress?.()
  }
  
  // 6.4 渲染
  return (
    <View className="event-card" onClick={handlePress}>
      {/* ... */}
    </View>
  )
}

// 7. 导出
export default EventCard
```

### 组件拆分原则

1. **单一职责**: 每个组件只做一件事
2. **行数限制**: 单个组件不超过 200 行
3. **复用性**: 超过 2 处使用的 UI 片段应抽取为组件
4. **Props 数量**: 单个组件 Props 不超过 10 个

---

## 🎨 样式规范

### SCSS 文件结构

```scss
// 1. 变量导入
@import '@/styles/variables.scss';

// 2. 组件根样式
.event-card {
  // 2.1 布局属性
  display: flex;
  flex-direction: column;
  
  // 2.2 盒模型属性
  padding: 24rpx;
  margin-bottom: 16rpx;
  
  // 2.3 视觉属性
  background: $light-card;
  border-radius: 16rpx;
  box-shadow: $light-shadow-card;
  
  // 2.4 过渡动画
  transition: all 0.3s ease;
  
  // 2.5 子元素样式
  .card-title {
    font-size: 32rpx;
    font-weight: 600;
  }
  
  // 2.6 状态样式
  &:active {
    transform: translateY(-4rpx);
  }
  
  // 2.7 响应式样式
  @media (min-width: 768px) {
    padding: 32rpx;
  }
}
```

### 主题适配规范

```scss
// 必须同时支持浅色和深色主题
.theme-light {
  .event-card {
    background: $light-card;
    color: $light-foreground;
  }
}

.theme-dark {
  .event-card {
    background: $dark-card;
    color: $dark-foreground;
  }
}
```

---

## 🔧 工具函数规范

### 函数模板

```typescript
/**
 * 格式化日期
 * @param date - 日期字符串或 Date 对象
 * @param format - 格式化模板，默认 'YYYY-MM-DD'
 * @returns 格式化后的日期字符串
 * @example
 * formatDate('2025-12-02') // '2025-12-02'
 * formatDate(new Date(), 'MM月DD日') // '12月02日'
 */
export function formatDate(date: string | Date, format = 'YYYY-MM-DD'): string {
  // 实现...
}
```

### 工具函数分类

| 文件 | 用途 |
|------|------|
| `utils/format.ts` | 格式化函数（日期、数字、文本） |
| `utils/storage.ts` | 本地存储操作 |
| `utils/validate.ts` | 表单验证函数 |
| `utils/request.ts` | HTTP 请求封装 |
| `utils/platform.ts` | 平台兼容性处理 |

---

## 📡 API 服务规范

### 服务文件模板

```typescript
// services/activities.ts

import { request } from '@/utils/request'
import type { Activity, ActivityListParams } from '@/types/activity'

/**
 * 获取活动列表
 */
export async function fetchActivities(params: ActivityListParams): Promise<Activity[]> {
  const response = await request.get('/activities', { params })
  return response.data
}

/**
 * 获取活动详情
 */
export async function fetchActivityById(id: string): Promise<Activity> {
  const response = await request.get(`/activities/${id}`)
  return response.data
}
```

---

## 🚫 禁止事项

### 代码层面

1. ❌ 禁止在单个文件中写超过 300 行代码
2. ❌ 禁止使用 `any` 类型（除非有充分理由）
3. ❌ 禁止在组件中直接调用 API（应通过 services）
4. ❌ 禁止硬编码颜色值（应使用变量）
5. ❌ 禁止使用内联样式（除非动态计算）

### 资源层面

1. ❌ 禁止使用 Emoji 作为图标
2. ❌ 禁止直接导入 SVG 文件
3. ❌ 禁止使用超过 100KB 的图片

---

## ✅ 代码审查清单

每次提交代码前，确认以下事项：

- [ ] 文件行数不超过 300 行
- [ ] 组件已正确拆分
- [ ] 公共函数已抽离到 utils
- [ ] 类型定义完整
- [ ] 支持双主题
- [ ] 无 Lint 错误
- [ ] 关键逻辑有注释

---

## 📝 变更记录

| 时间 | 变更内容 | 操作人 |
|------|----------|--------|
| 2025年12月02日 23:50 | 创建文档 | Cursor AI |

---

**下次更新**: 根据实际开发情况补充

