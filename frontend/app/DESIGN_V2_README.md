# 设计系统 V2 - 使用说明

## 📦 已创建的文件

```
frontend/app/src/pages/index/
├── index.tsx         ← V1（深绿色主题，当前使用）
├── index.scss        ← V1 样式
├── index-v2.tsx      ← V2（Modern B2B SaaS，新设计）✨
└── index-v2.scss     ← V2 样式 ✨
```

---

## 🎨 V2 设计特点

### 视觉风格
- **灵感来源**：Linear / Vercel / Candidate Assessment Tool
- **主题**：现代 B2B SaaS、简洁、极简主义
- **配色**：白色/浅灰色系（`#fff` / `#fafafa`）
- **重点**：内容为王，去除不必要的装饰

### 核心改进
1. ✨ **所有图标改为内联SVG**（无 .png 资源，体积更小）
2. ✨ **搜索框**：现代胶囊形状，浅灰背景
3. ✨ **活动卡片**：高级活动票证风格，柔和阴影
4. ✨ **状态标签**：优雅徽章（浅色背景+边框）
5. ✨ **配色系统**：统一灰阶，高可读性
6. ✨ **间距系统**：4px Grid，一致性强

---

## 🚀 如何预览 V2

### 方法 1：临时替换（推荐，用于快速预览）

**步骤 1**：备份当前文件
```bash
cd /Users/Python项目/活动报名系统/frontend/app/src/pages/index/
cp index.tsx index-v1-backup.tsx
cp index.scss index-v1-backup.scss
```

**步骤 2**：替换为 V2
```bash
cp index-v2.tsx index.tsx
cp index-v2.scss index.scss
```

**步骤 3**：编译运行
```bash
cd /Users/Python项目/活动报名系统/frontend/app/
pnpm run dev:weapp
```

**步骤 4**：在微信开发者工具中查看效果

**恢复 V1**：
```bash
cp index-v1-backup.tsx index.tsx
cp index-v1-backup.scss index.scss
```

---

### 方法 2：修改路由配置（永久切换）

**编辑 `app.config.ts`**：
```typescript
export default defineAppConfig({
  pages: [
    'pages/index/index-v2',  // ← 改为 index-v2
    'pages/search/index',
    'pages/activities/index',
    'pages/activity-detail/index',
    'pages/signup/index',
    'pages/profile/index',
    'pages/webview/index',
    'pages/ai-assistant/index'
  ],
  // ...
})
```

---

## 📊 V1 vs V2 对比

| 特性 | V1（深绿色主题） | V2（Modern B2B SaaS） |
|------|----------------|---------------------|
| **主色** | 深绿色 `#2D5A3D` | 白色/浅灰 `#fff`/`#fafafa` |
| **头部** | 深绿色渐变背景 | 纯白色背景 |
| **搜索框** | 半透明白边框 | 浅灰背景胶囊 |
| **图标** | PNG 图片（约 20KB） | SVG 内联（约 1KB） |
| **阴影** | 较重 | 柔和、扩散 |
| **状态标签** | 实色背景 | 浅色背景+边框 |
| **包大小** | 较大 | **更小** ✨ |

---

## 🎯 下一步计划

1. **预览 V2**：确认视觉风格是否符合预期
2. **收集反馈**：调整细节
3. **应用到其他页面**：
   - 搜索页面
   - 活动列表页
   - 活动详情页
   - 报名表单
   - 我的页面

---

## 📝 设计文档

完整设计规范请参考：
- `docs/DESIGN_SYSTEM_V2.md` - 设计系统完整文档
- 包含：配色、间距、字体、圆角、阴影、组件规范

---

**Need Help?** 随时反馈您的想法和建议！🎨


