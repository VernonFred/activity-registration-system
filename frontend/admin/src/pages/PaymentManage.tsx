import PageHeader from '../components/PageHeader'
import SectionCard from '../components/SectionCard'

export default function PaymentManage() {
  return (
    <div>
      <PageHeader title="缴费管理" subtitle="管理员侧支付记录、确认与退款（Phase 0 占位）" />
      <SectionCard>
        已纳入后台信息架构。下一阶段将接入统一 DataTable、筛选器与详情抽屉。
      </SectionCard>
    </div>
  )
}
